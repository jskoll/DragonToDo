; No opcode caching of file contents in dev, so bind-mounted source edits are picked
; up immediately (validate_timestamps=1 is the default here — just being explicit).
opcache.validate_timestamps=1
opcache.revalidate_freq=0

display_errors=1
error_reporting=E_ALL
